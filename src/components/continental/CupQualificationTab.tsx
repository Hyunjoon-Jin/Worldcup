import { GlassCard } from '../common/GlassCard'

/** 대륙컵 지역예선 최상위 탭 — Phase C에서 조추첨/일정·진행/조별 순위를 월드컵 예선 수준으로 채운다. */
export function CupQualificationTab() {
  return (
    <GlassCard className="p-8 text-center text-sm text-gray-400">
      🌍 대륙컵 지역예선 화면을 준비 중입니다. 각 대륙컵의 예선(조추첨·일정·조별 순위)을 여기에서 볼 수 있게 됩니다.
    </GlassCard>
  )
}
